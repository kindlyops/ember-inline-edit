machine:
  node:
    version: 4.6.2

dependencies:
  pre:
    - npm install -g bower
    - npm install bower
    - npm install
    - bower install
  cache_directories:
    - "node_modules"
    - "bower_components"
